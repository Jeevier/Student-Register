import styled from "styled-components";

const Card = styled.div`
  background: white;
  border-radius: 5px;
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  color: black;
  padding: 20px;
  margin: 2rem auto;
`;

export default Card;
